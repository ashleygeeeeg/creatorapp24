# Web app loaded inside the WebView (HTTPS recommended for Expo Go)
EXPO_PUBLIC_WEB_URL=https://your-app.vercel.app

# Local only — use your computer's LAN IP, not localhost:
# EXPO_PUBLIC_WEB_URL=http://192.168.1.42:3000

# Optional after `eas init`:
# EXPO_PUBLIC_EAS_PROJECT_ID=
